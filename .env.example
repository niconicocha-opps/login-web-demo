# Login credentials (both must be set to override defaults)
LOGIN_USERNAME=admin
LOGIN_PASSWORD=your-password-here

# Flask secret key (change in production)
SECRET_KEY=change-this-to-a-random-secret-key
